# Vercel KV (Upstash) — used for waitlist storage. Optional in dev.
# When unset, the waitlist API gracefully no-ops and returns ok:true.
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Public site URL (used in metadata, OG, JSON-LD, sitemap)
NEXT_PUBLIC_SITE_URL=https://mizan.app
